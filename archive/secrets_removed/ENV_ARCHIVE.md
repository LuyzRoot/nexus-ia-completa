# Archived secrets removed from tree

This folder stores `.env` files that were previously committed. They may contain placeholders or sample values. Sensitive values have been removed and replaced with placeholders.

Original file moved here: `.env` (from repository root).
If you need to restore, copy the file back and ensure secrets are replaced.
